---
import { getSession } from "../../lib/session";

const session = getSession(Astro);
if (!session?.isAdmin) {
  return Astro.redirect("/auth/login");
}

return Astro.redirect("/admin/role-settings");
---